# Generated by syda  |  model: grok-3  |  2026-06-08T14:19:23 UTC
# Edit freely — syda will use your changes on the next run.
# Columns with no generate_* function here are generated via LLM.

def generate_order_id(row, col_name):
    return row.name + 1

def generate_customer_email(row, col_name):
    import random
    domains = ['example.com', 'mail.com', 'shop.net']
    return f'user{random.randint(1000,9999)}@{random.choice(domains)}'

def generate_order_date(row, col_name):
    import random
    from datetime import datetime, timedelta
    end = datetime.now()
    start = end - timedelta(days=730)
    delta = end - start
    random_date = start + timedelta(days=random.randint(0, delta.days))
    return random_date.strftime('%Y-%m-%d')

def generate_status(row, col_name):
    import random
    statuses = ['pending', 'processing', 'shipped', 'delivered', 'cancelled']
    return random.choice(statuses)

def generate_quantity(row, col_name):
    import random
    return random.randint(1, 10)

def generate_total_amount(row, col_name):
    import random
    return round(random.uniform(0.0, 500.0), 2)
